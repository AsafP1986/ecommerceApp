import { CreditcardpipePipe } from './creditcardpipe.pipe';

describe('CreditcardpipePipe', () => {
  it('create an instance', () => {
    const pipe = new CreditcardpipePipe();
    expect(pipe).toBeTruthy();
  });
});
