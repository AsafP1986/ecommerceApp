import { CreditcarddirectiveDirective } from './creditcarddirective.directive';

describe('CreditcarddirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CreditcarddirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
